#include "philosophers.h"

int main(int argc, char *argv[])
{
    t_table table;

    if (argc != 5)
        return (printf("Error: Invalid input\n"));
    else
    {
        parse(&table, argv)
        
    }

}